import {Validator} from "./validator";
import {RULES_VALIDATION} from "./utils";

export {
  /**
   * @class Validator
   *
   * @method check
   * @param {string} name
   * @param {string} data
   * @return {{passed: boolean, errors: *[]}}
   */
  Validator,
  RULES_VALIDATION
};
