import {Validator} from "./validator";
import {RULES_HANDLERS} from "./utils";

export {
  /**
   * @class Validator
   *
   * @method check
   * @param {string} name
   * @param {string} data
   * @return {{passed: boolean, errors: *[]}}
   */
  Validator,
  RULES_HANDLERS
};
